/**
 * The login form's decoy field, shared by the page that renders it and the
 * route that checks it. Named like something a form-filling bot will happily
 * complete; people never see it, and password managers do not fill it.
 */
export const HONEYPOT_FIELD = "website";
